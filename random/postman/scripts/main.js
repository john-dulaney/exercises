// run a for each loop over the return. print it to the dom.


let 
function (getCommit) {
    return $.ajax({
        "url": `https://api.github.com/repos/nss-day-cohort-22/movie-history-wiggly-donkeys/commits`,
        "method": "GET"

    }).then( 
        function(response)
        $("ayyjax")
        commit.message
response.commit.author.name
        
        )}